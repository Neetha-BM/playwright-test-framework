[pytest]
testpaths = tests
markers =
    smoke: Quick sanity tests
    regression: Full regression suite
    login: Login feature tests
    cart: Shopping cart tests
    checkout: Checkout flow tests
    local: Tests against local demo app
addopts = --strict-markers
